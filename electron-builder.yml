appId: 'kr.modakbul.season1.launcher'
productName: '모닥불 Season 1'
artifactName: 'Modakbul-Season-1-setup-${version}.${ext}'

copyright: 'Copyright © 2026 모닥불 Season 1 운영팀'

asar: true
compression: 'maximum'
afterPack: 'tools/after-pack.js'

files:
  - '**/*'
  # electron-builder normally discovers production dependencies itself.  The
  # portable build environment used for this launcher can skip that discovery,
  # leaving app.asar without runtime modules.  build-electron.js prepares the
  # exact production dependency closure in runtime_node_modules instead.
  - '!{dist,dist-*,node_modules,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,eslint.config.mjs,runtime_node_modules}'
  - from: 'runtime_node_modules'
    to: 'node_modules'
    filter:
      - '**/*'

extraResources:
  - 'libraries'

# Windows Configuration
win:
  # This release is unsigned. electron-builder's combined signing/resource
  # editing helper downloads a cross-platform archive containing Unix symlinks,
  # which cannot be unpacked on standard Windows accounts. after-pack.js applies
  # the icon and product metadata with the already bundled rcedit instead.
  icon: 'build/icon.png'
  signAndEditExecutable: false
  target: 
    - target: 'nsis'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: false
  createDesktopShortcut: always
  createStartMenuShortcut: true
  shortcutName: '모닥불 Season 1'

# macOS Configuration
mac:
  icon: 'build/icon-mac.png'
  target:
    - target: 'dmg'
      arch:
        - 'x64'
        - 'arm64'
  artifactName: '${productName}-setup-${version}-${arch}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: '모닥불 Season 1 운영팀'
  vendor: '모닥불 Season 1'
  synopsis: '모닥불 Season 1 전용 Minecraft 런처'
  description: '모드, 설정, 리소스팩과 업데이트를 자동으로 관리하는 모닥불 Season 1 전용 런처입니다.'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'
